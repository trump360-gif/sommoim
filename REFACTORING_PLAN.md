# Sommoim 리팩터링 작업 계획서

## 현재 상태 분석 결과

### 확인된 문제점

| # | 문제 | 현재 상태 | 심각도 |
|---|------|----------|--------|
| 1 | event-list.tsx 과대 | 695줄 | 🔴 높음 |
| 2 | staff-management.tsx 과대 | 597줄 | 🔴 높음 |
| 3 | formatDate 중복 | 13개 파일에서 사용, 최소 3곳 정의 | 🟡 중간 |
| 4 | categoryLabels 중복 | 4개 파일에서 중복 정의 | 🟡 중간 |
| 5 | statusLabels 중복 | 8개 파일에서 사용, 중복 정의 존재 | 🟡 중간 |
| 6 | EventList 중복 | `_components/event-list.tsx` 별도 존재 | 🟡 중간 |
| 7 | ImageUpload 중복 | image-upload.tsx + image-uploader.tsx | 🟢 낮음 |
| 8 | shared/ 미존재 | 공통 모듈 디렉토리 없음 | 🟢 낮음 |

---

## 작업 계획

### Phase 1: 기반 작업 (의존성 해결 우선)

#### Task 1.1: shared/ 디렉토리 구조 생성
**예상 시간:** 30분

```
frontend/src/shared/
├── index.ts
├── constants/
│   ├── index.ts
│   ├── category.ts
│   └── status.ts
├── utils/
│   ├── index.ts
│   └── format.ts
└── hooks/
    └── index.ts
```

#### Task 1.2: formatDate 통합
**예상 시간:** 1시간

**현재 정의 위치:**
- `src/lib/utils.ts` (기본)
- `src/app/meetings/[id]/_components/constants.ts` (변형)
- 각 컴포넌트 내부

**작업 내용:**
1. `shared/utils/format.ts` 생성
2. 모든 formatDate 변형 통합
3. 13개 파일 import 경로 변경
4. 기존 중복 코드 삭제

**통합 API:**
```typescript
// shared/utils/format.ts
export function formatDate(date: Date | string, options?: FormatDateOptions): string;
export function formatEventDate(dateStr: string): EventDateParts;
export function formatRelativeTime(date: Date | string): string;
```

#### Task 1.3: Constants 통합
**예상 시간:** 1시간

**통합 대상:**
- `categoryLabels` / `CATEGORY_LABELS`
- `statusLabels` / `STATUS_LABELS`
- `eventTypeLabels`
- `participantStatusLabels`

**작업 내용:**
1. `shared/constants/category.ts` 생성
2. `shared/constants/status.ts` 생성
3. 기존 정의 삭제 및 import 변경

---

### Phase 2: 컴포넌트 중복 제거

#### Task 2.1: EventList 통합
**예상 시간:** 1시간

**현재 상태:**
- `src/components/meeting/event-list.tsx` (695줄) - 공유 버전
- `src/app/meetings/[id]/_components/event-list.tsx` (273줄) - 페이지 전용

**작업 내용:**
1. 두 컴포넌트 기능 비교 분석
2. 공유 버전을 기준으로 통합
3. 페이지 전용 버전 삭제
4. Import 경로 수정

#### Task 2.2: ImageUpload 정리
**예상 시간:** 1시간

**현재 상태:**
- `image-upload.tsx` (292줄) - URL 입력 + 파일
- `image-uploader.tsx` (208줄) - 파일 업로드 전문

**권장 방안:** 통합 (A안)
1. `image-upload.tsx`를 기본으로 유지
2. `image-uploader.tsx`의 고급 기능 병합
3. Props로 기능 토글
4. `image-uploader.tsx` deprecated 후 삭제

---

### Phase 3: 대형 컴포넌트 분리

#### Task 3.1: event-list.tsx 분리 (695줄)
**예상 시간:** 2시간

**분리 구조:**
```
src/components/meeting/event-list/
├── index.tsx          (~150줄) - 메인 컴포넌트
├── event-filters.tsx  (~100줄) - 필터 UI
├── event-modal.tsx    (~200줄) - 생성/수정 모달
├── event-card.tsx     (~100줄) - 이벤트 카드
├── types.ts           (~50줄)  - 타입 정의
└── use-events.ts      (~80줄)  - 커스텀 훅
```

**분리 기준:**
- UI 로직 분리 (필터, 카드, 모달)
- 상태 관리 훅 추출
- 타입 별도 파일

#### Task 3.2: staff-management.tsx 분리 (597줄)
**예상 시간:** 2시간

**분리 구조:**
```
src/components/meeting/staff-management/
├── index.tsx           (~100줄) - 메인 컴포넌트
├── staff-list.tsx      (~150줄) - 스태프 목록
├── add-staff-modal.tsx (~150줄) - 추가 모달
├── staff-card.tsx      (~100줄) - 스태프 카드
├── types.ts            (~30줄)  - 타입 정의
└── use-staff.ts        (~60줄)  - 커스텀 훅
```

---

### Phase 4: 정리 및 검증

#### Task 4.1: Import 경로 정리
**예상 시간:** 30분

- 모든 파일에서 `@/shared` 사용 확인
- 절대 경로 일관성 검증
- 불필요한 re-export 제거

#### Task 4.2: 빌드 및 테스트
**예상 시간:** 30분

- TypeScript 빌드 검증
- 런타임 오류 확인
- 기능 테스트

---

## 작업 우선순위 (권장 순서)

| 순서 | 작업 | 예상 시간 | 이유 |
|------|------|----------|------|
| 1 | Task 1.1 shared/ 생성 | 30분 | 다른 작업의 기반 |
| 2 | Task 1.2 formatDate 통합 | 1시간 | 가장 많이 사용되는 유틸 |
| 3 | Task 1.3 Constants 통합 | 1시간 | 중복 제거 효과 큼 |
| 4 | Task 2.1 EventList 통합 | 1시간 | 분리 작업 전 정리 |
| 5 | Task 3.1 event-list 분리 | 2시간 | 가장 큰 파일 |
| 6 | Task 3.2 staff-management 분리 | 2시간 | 두 번째로 큰 파일 |
| 7 | Task 2.2 ImageUpload 정리 | 1시간 | 낮은 우선순위 |
| 8 | Task 4.1-4.2 정리 및 검증 | 1시간 | 마무리 |

**총 예상 시간:** 약 10시간

---

## 성공 지표

### 정량적 목표
- [ ] 500줄 초과 파일: 2개 → 0개
- [ ] formatDate 정의 위치: 3곳 → 1곳
- [ ] Constants 중복: 4곳 → 1곳
- [ ] EventList 컴포넌트: 2개 → 1개
- [ ] ImageUpload 컴포넌트: 2개 → 1개

### 정성적 목표
- [ ] shared/ 디렉토리를 통한 명확한 공통 모듈 관리
- [ ] Import 경로 일관성 확보
- [ ] 코드 재사용성 향상
- [ ] 유지보수 용이성 개선

---

## 리스크 및 대응

| 리스크 | 확률 | 영향 | 대응 방안 |
|--------|------|------|----------|
| Import 경로 누락 | 중간 | 빌드 실패 | 빌드 검증 후 진행 |
| 기능 누락 | 낮음 | 런타임 오류 | 분리 전 기능 목록 작성 |
| 타입 불일치 | 중간 | TS 오류 | 공통 타입 먼저 정의 |

---

## 결론

현재 코드베이스는 **중복 코드와 과대 파일**이 주요 기술 부채입니다.

- **즉시 착수 권장:** Task 1.1 ~ 1.3 (기반 작업)
- **효과 대비 작업량:** formatDate 통합이 가장 효율적
- **최우선 분리 대상:** event-list.tsx (695줄)

이 리팩터링을 완료하면:
1. 파일당 최대 200줄 이하 유지
2. 공통 코드 단일 소스화
3. 향후 기능 추가 시 일관된 패턴 적용 가능
